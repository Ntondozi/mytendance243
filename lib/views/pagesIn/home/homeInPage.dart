import 'package:flutter/material.dart';

class Homeinpage extends StatelessWidget {
  const Homeinpage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(child: Text('data'),),
    );
  }
}